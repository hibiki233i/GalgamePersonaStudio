<Window x:Class="GalgamePersonaStudio.ChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择肢"
        Width="520" MaxHeight="450"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="#F0F4F8">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="检测到选择肢，请选择一个选项："
                   FontWeight="SemiBold" FontSize="14" Foreground="#1A202C" Margin="0,0,0,12"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ChoiceList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Text}"
                                Tag="{Binding Index}"
                                Click="ChoiceButton_Click"
                                Margin="0,0,0,8"
                                Padding="14,12"
                                FontSize="14"
                                Background="#FFFFFF"
                                Foreground="#1A202C"
                                BorderBrush="#CBD5E1"
                                BorderThickness="1"
                                Cursor="Hand"
                                HorizontalContentAlignment="Left"
                                HorizontalAlignment="Stretch">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#EFF6FF"/>
                                            <Setter Property="BorderBrush" Value="#3B82F6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="2" Content="跳过 (不选择)" Click="Cancel_Click"
                Padding="16,8" Margin="0,12,0,0"
                Background="#FFFFFF" Foreground="#1A202C" BorderBrush="#CBD5E1" BorderThickness="1"
                Cursor="Hand" HorizontalAlignment="Right"/>
    </Grid>
</Window>
